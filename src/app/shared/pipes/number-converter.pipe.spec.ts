import { NumberConverterPipe } from './number-converter.pipe';

describe('NumberConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
